//C

#include <string.h>
int lengthOfLastWord(char* s) {
    int j = strlen(s) - 1, word = 0;
    for(int i = j; i >= 0 && (!word || s[i] != ' '); i--) if(s[i] != ' ') {
        word = 1;
        j--;
    }
    return strlen(s) - j - 1;
}
