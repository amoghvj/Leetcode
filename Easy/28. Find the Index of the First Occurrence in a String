//C

int strStr(char* haystack, char* needle) {
    for(int i = 0; haystack[i] != '\0'; i++) {
        for(int j = 0; true; j++) {
            if(needle[j] == '\0') return i; 
            if(haystack[i + j] == '\0' || needle[j] != haystack[i + j]) break;
        }
    }
    return -1;
}
