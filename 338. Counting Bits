//Java

class Solution {
    public int[] countBits(int n) {
        int arr[] = new int[n + 1];
        for (int i = 0; i <= n; i++) {
            arr[i] = 0;
            for (int j = i; j != 0; arr[i] += j % 2, j /= 2)
                ;
        }
        return arr;
    }
}
