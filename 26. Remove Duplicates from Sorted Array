//C

int removeDuplicates(int* nums, int numsSize) {
    int i = 0, j = 0;
    do {
        if(nums[i] != nums[j]) {
            nums[j + 1] = nums[i];
            j++;
        }
        i++;
    }
    while(i < numsSize);
    return j + 1;
}
